{% extends 'base.html.twig' %}

{% block title %}Nouveau commentaire · {{ comment.post.name }} · Blog{% endblock %}

{% block body %}
    <div class="body-content">
        <h1>Create new Comment</h1>

        {{ include('comment/_form.html.twig') }}

        <a href="{{ path('comment_user_index') }}">back to list</a>
    </div>
{% endblock %}
