{% extends 'base.html.twig' %}

{% block title %}Modification · Commentaire {{ comment.id }} · {{ comment.post.name }} · Blog{% endblock %}

{% block body %}
    <div class="body-content">
        <h1>Edit Comment</h1>

        {{ include('comment/_form.html.twig', {'button_label': 'Update'}) }}

        <a href="{{ path('comment_admin_index', {'id': post.id, 'slug': post.slug}) }}">Retour à la liste</a>

        {{ include('comment/_delete_form.html.twig') }}
    </div>
{% endblock %}
